/**
 * Save Modal Handler
 *
 * Class used for handling all the actions for a Save Modal.
 *
 * Dependencies:
 *   > Javascript ES6
 *   > jQuery
 *
 * Written in:
 *   > Javascript ES8
 *   > jQuery 3.4.1
 *
 * @TODO Form Validator (extra class)
 */
// FormHandler.js
// Form.js (entity) extends validation.js
class SaveModalHandler
{
    /*
    class Person {
  constructor(name) {
    this._name = name;
  }

  get name() {
    return this._name;
  }

  set name(newName) {
    this._name = newName;
  }

  walk() {
    console.log(this._name + ' is walking.');
  }
}
*/
    form = null;
    url  = null;
    options = null;

    constructor()
    {
        // Nieuwe form aanmaken
        console.log('Constructed');
        // options parsen met getters en setters
        // url hierna ook
    }

    // event listeners zetten

    // geen ajax maar fetch()
}
